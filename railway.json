{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "releaseCommand": "php artisan optimize && php artisan view:cache && php artisan event:cache && php artisan migrate --force && php artisan storage:link"
  }
}
